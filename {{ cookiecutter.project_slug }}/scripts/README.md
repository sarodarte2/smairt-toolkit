# Helper Scripts

Utility scripts to support the SMAIRT workflow.

---

## Included Scripts

### compile_for_ai.py

Compiles the current project state into a single document that can be pasted into a new AI session.

This is an essential part of the SMAIRT workflow - leaving a 'breadcrumb' trail
that allows you to A) track what you have done and keep a record of those
steps, results, and interpretation as you go along, B) provide a record that
can be used to feed back in to an AI to bring it up to speed on what was done,
for what reason, and what the results were.

**Usage:**
```bash
python scripts/compile_for_ai.py
```

**Output:**
- Creates `prompts/compiled_for_ai.md`
- Prints instructions for copying to clipboard

---

### new_script.py

Creates a new numbered script with the standard SMAIRT template. Auto-detects the next script number based on existing scripts.

**Usage:**
```bash
python scripts/new_script.py
```

**Prompts for:**
- Phase (synthetic / downloaded / real)
- Script description
- Hypothesis being tested
- Iteration number

**Creates:**
- A new script in the appropriate `experiments/` subfolder
- With logging setup already configured
- With the results comment block for pasting output

---

## Other Suggested Scripts

The following scripts are not included but can be generated by AI on demand. Copy the associated prompt into your AI session to create these scripts as needed.

---

### generate_summary.py

Generates a summary of what's been tried across all iterations—experiments run, hypotheses tested, results obtained, limitations identified.

**Prompt to generate this script:**
```
Please create a Python script called generate_summary.py that:

1. Scans through all scripts in experiments/01_synthetic/, experiments/02_downloaded/, and experiments/03_real_data/
2. Extracts the docstrings (hypothesis, phase, iteration) from each script
3. Extracts the pasted output comment blocks from each script
4. Reads hypotheses/hypothesis_log.md for hypothesis status (supported/not supported)
5. Reads analysis/iteration_log.md for limitations identified
6. Compiles everything into a summary document

Output should include:
- List of all experiments run with their hypotheses and results
- Which hypotheses were supported vs. not supported
- Key limitations identified
- Current phase and iteration

Save the output to analysis/project_summary.md

Follow the SMAIRT code conventions in prompts/CODE_CONVENTIONS.md
```

---

### validate_structure.py

Checks that the project structure follows SMAIRT conventions—properly named scripts, log files matching scripts, required files present.

**Prompt to generate this script:**
```
Please create a Python script called validate_structure.py that checks SMAIRT project conventions:

1. Check that all scripts in experiments/ follow naming convention: script_XX_description.py
2. Check that each script has a corresponding log file in results/logs/
3. Check that required files exist:
   - prompts/AI_CONTEXT.md
   - prompts/CODE_CONVENTIONS.md
   - prompts/session_log.md
   - prompts/intellectual_contribution.md
   - background/01_initial_question.md
   - hypotheses/hypothesis_log.md
   - analysis/iteration_log.md
   - analysis/future_directions.md
4. Check that scripts have the pasted output comment block at the end
5. Report any issues found

Output a validation report to console showing:
- ✓ for passing checks
- ✗ for failing checks with details on what's wrong

Follow the SMAIRT code conventions in prompts/CODE_CONVENTIONS.md
```

---

### extract_results.py

Parses through all scripts and extracts the pasted output comment blocks, compiling them into a single results document.

**Prompt to generate this script:**
```
Please create a Python script called extract_results.py that:

1. Scans all scripts in experiments/01_synthetic/, experiments/02_downloaded/, experiments/03_real_data/
2. Extracts the pasted output comment blocks from each script (the section after "# === PASTE OUTPUT HERE ===")
3. For each script, captures:
   - Script name and path
   - Phase (synthetic/downloaded/real)
   - Hypothesis (from docstring)
   - The OUTPUT section
   - The INTERPRETATION section
   - The NEXT STEPS section
4. Compiles into a single markdown document organized by phase and iteration

Save output to analysis/extracted_results.md

This creates a single document showing all results without needing to open each script.

Follow the SMAIRT code conventions in prompts/CODE_CONVENTIONS.md
```

---

### download_benchmark.py

Helper for downloading common benchmark datasets into the `data/downloaded/` folder.

**Prompt to generate this script:**
```
Please create a Python script called download_benchmark.py that:

1. Presents a menu of common benchmark datasets:
   - Iris dataset (classification)
   - Wine dataset (classification)
   - Boston Housing (regression)
   - MNIST subset (image classification)
   - Titanic (classification with missing data)
   - California Housing (regression)
   - [Add others appropriate to my domain: {{ cookiecutter.domain }}]

2. Downloads selected dataset(s) to data/downloaded/
3. Creates a README in data/downloaded/ describing each dataset
4. Prints basic statistics about the downloaded data

"Have Claude and the scripts download test data—it's got a really great library because lots of people are doing this kind of thing from different disciplines. It gives you diversity—easy data, hard data, messy experimental data, cleaner data like Iris. For fundamental algorithm development, it makes sense to have datasets from different disciplines to show it's robust."

Follow the SMAIRT code conventions in prompts/CODE_CONVENTIONS.md
```

---

### generate_synthetic.py

Template script for generating synthetic data with configurable parameters.

**Prompt to generate this script:**
```
Please create a Python script called generate_synthetic.py that:

1. Generates synthetic data appropriate for testing {{ cookiecutter.initial_research_question }}
2. Allows configurable parameters:
   - Number of samples
   - Noise level
   - Complexity/difficulty
   - Random seed for reproducibility
3. Saves data to data/synthetic/ with descriptive filename
4. Creates a README describing the synthetic data parameters
5. Prints summary statistics

"Synthetic data is really useful—it's very easy to do in this cycle because you're not dependent on large datasets. You can get an idea of what might work and what might not just from the code itself. You're not dependent on outside sources and you can iterate in this tight loop."

Follow the SMAIRT code conventions in prompts/CODE_CONVENTIONS.md
```

---

### update_hypothesis_log.py

Interactive script to add a new hypothesis entry to the hypothesis log.

**Prompt to generate this script:**
```
Please create a Python script called update_hypothesis_log.py that:

1. Prompts for:
   - Hypothesis statement
   - Iteration number
   - Related experiment scripts
   - Status (testing / supported / not supported / partially supported / inconclusive)
   - Evidence summary
   - Interpretation
   - What hypothesis this leads to next

2. Appends a properly formatted entry to hypotheses/hypothesis_log.md
3. Maintains consistent formatting with existing entries

Follow the SMAIRT code conventions in prompts/CODE_CONVENTIONS.md
```

---

### log_intellectual_contribution.py

Prompts user to document their intellectual contributions for the current iteration.

**Prompt to generate this script:**
```
Please create a Python script called log_intellectual_contribution.py that:

1. Prompts for:
   - Iteration number and date
   - Phase (synthetic/downloaded/real)
   - Hypothesis being tested
   - What AI suggested
   - What I suggested
   - Critical insights I made
   - Decisions I made
   - Where I pushed past a dead end

2. Appends a properly formatted entry to prompts/intellectual_contribution.md

"It records what your intellectual contribution to the effort is, because if AI is just generating these ideas and testing them all by itself and moving things forward, your intellectual contribution might be that you pressed the button. But you really need to know where you made those critical steps."

Follow the SMAIRT code conventions in prompts/CODE_CONVENTIONS.md
```

---

### export_for_paper.py

Compiles selected results, figures, and narratives into a format suitable for starting a paper draft.

**Prompt to generate this script:**
```
Please create a Python script called export_for_paper.py that:

1. Prompts user to select which experiments/results to include
2. Gathers from the project:
   - Background/research question from background/01_initial_question.md
   - Key hypotheses and their outcomes from hypotheses/hypothesis_log.md
   - Selected results and interpretations
   - Figures from results/figures/
   - Methods descriptions from scripts
   - Future directions from analysis/future_directions.md

3. Compiles into paper_draft/export/ with:
   - introduction_draft.md
   - methods_draft.md
   - results_draft.md
   - discussion_draft.md
   - figures/ (copied relevant figures)

This follows the scientific method structure where each section maps to a part of the paper.

Follow the SMAIRT code conventions in prompts/CODE_CONVENTIONS.md
```

---

### archive_iteration.py

Archives a completed iteration, creating a snapshot that can be referenced later.

**Prompt to generate this script:**
```
Please create a Python script called archive_iteration.py that:

1. Prompts for iteration number to archive
2. Creates an archive folder: archives/iteration_XX_YYYY-MM-DD/
3. Copies relevant files:
   - All scripts from that iteration
   - Corresponding log files
   - Relevant hypothesis log entries
   - Session log entries for that iteration
   - Intellectual contribution entries
   - Any figures generated
4. Creates a summary README in the archive folder
5. Optionally cleans up working directories

This keeps the working directory clean while preserving the full history.

Follow the SMAIRT code conventions in prompts/CODE_CONVENTIONS.md
```

---

## The Template System

The SMAIRT template system is set up on the idea of iterating through
an overall driving question with a series of iterations that are all based
on the scientific method. It is set up for rapidly exploring the feasibility
of an idea, to test the straightforward ideas and capture results to understand
where the real frontiers are and identify gaps that require innovation.
---

## The 4 Pieces of Information

The SMAIRT system records 4 different pieces of information in separate files:

1. **Background** - The question that went into prompting it, what has been done on that area, what's known about that question from literature, and a summary of the previous results

2. **Hypothesis** - Could be in a separate file or at the end of the background

3. **Methods** - The actual code and the data required to run the experiment

4. **Results + Interpretation** - The log file output plus interpretation through the lens of the hypothesis

---

## Why Generate Scripts On Demand?

Rather than including all possible helper scripts, SMAIRT provides prompts to generate them as needed. This approach:

1. **Reduces bloat** - You only create what you actually need
2. **Allows customization** - Scripts are tailored to your specific project
3. **Follows SMAIRT philosophy** - Use AI as a tool to generate code in context
4. **Avoids maintenance burden** - No need to update scripts you don't use
